## Archivo de ejemplo de variables de entorno
# Copia este archivo a `.env` y reemplaza los valores por los reales.

API_URL=https://api.example.com/upload
API_KEY=your_api_key_here
ENVIRONMENT=development

# Ejemplo de uso:
# - `API_URL` apunta al endpoint que recibirá la imagen multipart.
# - `API_KEY` será enviado en el header `Authorization: Bearer <API_KEY>` si no
#   existe una API key guardada en el almacenamiento seguro de la app.
